## Analysis Questions

1. Head in linked list acts as an entry point for the data stored in the list. If we lose the head, we lose the entire list.
   Even if the nodes still exist in memory, there have no way to access them.
2. Because it needs that to point to the next node (a pointer).
3. Because in an array we need to move the entirety of the array and then we can add value at the beginning of the array.
   Whereas in a linked list data structure, Create a new node and point the new node's "next" to the current head.
4. The first line is to add a new head to the list. And the second one is to update the head so that the "newNode" is the new head.
5. The Program will not print out the last added value to the list.
6. Array is better for fast random access. Because we can just point to the array's prefered index and instantly get what we wanted.
7. Linked List is better. Because it has constant 2 step value insertion in the beginning. Whereas for array, we need to move n sized of data
   and then add the value to the beginning.
8. Because each elements requires extra space for the pointers.
